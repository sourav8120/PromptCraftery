{
  "version": 2,
  "experimentalServices": {
    "frontend": {
      "command": "cd frontend && npm install && npm run build",
      "port": 3000
    },
    "admin": {
      "command": "cd admin && npm install && npm run build",
      "port": 3001
    },
    "backend": {
      "command": "cd backend && npm install && npm run start",
      "port": 5001
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "http://localhost:5001/api/$1"
    }
  ]
}
